
export default function({fileList}:any){
    return <div className="h-[300px] overflow-y-scroll">
        {
            fileList.map((file:any)=>
            <div className="h-[40px] text-yellow-400">{file.name}</div>
            )
        }
        {
            fileList.map((file:any)=>
            <div className="h-[40px] text-yellow-400">{file.name}</div>
            )
        }
        {
            fileList.map((file:any)=>
            <div className="h-[40px] text-yellow-400">{file.name}</div>
            )
        }
        {
            fileList.map((file:any)=>
            <div className="h-[40px] text-yellow-400">{file.name}</div>
            )
        }
        {
            fileList.map((file:any)=>
            <div className="h-[40px] text-yellow-400">{file.name}</div>
            )
        }
        {
            fileList.map((file:any)=>
            <div className="h-[40px] text-yellow-400">{file.name}</div>
            )
        }
        {
            fileList.map((file:any)=>
            <div className="h-[40px] text-yellow-400">{file.name}</div>
            )
        }
        {
            fileList.map((file:any)=>
            <div className="h-[40px] text-yellow-400">{file.name}</div>
            )
        }
        {
            fileList.map((file:any)=>
            <div className="h-[40px] text-yellow-400">{file.name}</div>
            )
        }
    </div>
}